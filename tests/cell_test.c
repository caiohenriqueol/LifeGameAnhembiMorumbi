#include "cell.h"

int SIZE_X = 10;
int SIZE_Y = 10;
void isAliveTest() {
	LIMIT_CELLS= 10;
	cell *cells[SIZE_X][SIZE_Y]; 
	int x, y;
	bool result = isAlive(cells, x, y);
} 

void create_cell(*cells[SIZE_X][SIZE_Y] cells) {
	int contador = 0;
	for(contador = 0, contador <  SIZE_X) {}
}

